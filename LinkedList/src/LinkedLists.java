import java.util.Arrays;
import java.util.LinkedList;
import java.util.List;

public class LinkedLists {
    List sortedList_1 = new LinkedList(Arrays.asList(2, 3, 6, 7, 8, 10));
    List sortedList_2 = new LinkedList(Arrays.asList(-1, 3, 6, 7, 9));

    List unSortedList_1 = new LinkedList(Arrays.asList(12, -3, 6, 17, 18, 10));
    List unSortedList_2 = new LinkedList(Arrays.asList(-1, 3, 16, 17, 9));
}
